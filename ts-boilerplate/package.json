{
  "scripts": {
    "run:local": "tsx watch src/main.ts",
    "run:deploy": "pm2-runtime start dist/main.js -i max",
    "build": "tsc -p .",
    "prettier": "prettier --write src/"
  },
  "dependencies": {
    "dotenv": "^16.3.1",
    "fastify": "^4.19.1",
    "pino": "^8.14.1",
    "pino-pretty": "^10.0.0",
    "pm2": "^5.3.0"
  },
  "devDependencies": {
    "@types/node": "^20.3.3",
    "prettier": "2.8.8",
    "tsx": "^3.12.7",
    "typescript": "^5.1.6"
  }
}
